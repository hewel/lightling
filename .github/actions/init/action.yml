name: Install deps
description: ''
runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: '22.12'

    - name: Install packages
      shell: bash
      run: npm install
